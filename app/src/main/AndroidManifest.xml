<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/Theme.Main">
        <activity
            android:name="fr.isep.workoutapp.presentation.ui.MainHome"
            android:exported="false" />

        <activity
            android:name="fr.isep.workoutapp.presentation.ui.ExerciseCategory"
            android:exported="false" />
        <activity
            android:name="fr.isep.workoutapp.presentation.ui.ContactUs"
            android:exported="false" />
        <activity
            android:name="fr.isep.workoutapp.presentation.ui.AboutUs"
            android:exported="false" />
        <activity
            android:name="fr.isep.workoutapp.presentation.ui.HomeScreen"
            android:exported="false" /> <!-- SplashScreen Activity set as the launcher activity -->
        <activity
            android:name="fr.isep.workoutapp.presentation.ui.SplashScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- MainActivity -->
        <activity
            android:name="fr.isep.workoutapp.presentation.ui.MainActivity"
            android:exported="true">

            <!-- Removed the launcher intent filter from MainActivity -->
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>